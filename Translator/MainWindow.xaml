<Window x:Class="Translator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Переводчик" Height="450" Width="800"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Язык оригинала:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <ComboBox x:Name="SourceLanguageComboBox" Width="150" Margin="0,0,20,0"/>
            
            <TextBlock Text="Язык перевода:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <ComboBox x:Name="TargetLanguageComboBox" Width="150" Margin="0,0,20,0"/>
            
            <Button x:Name="SwapLanguagesButton" Content="↔" Width="30" Margin="0,0,20,0"
                    ToolTip="Поменять языки местами"/>
            
            <Button x:Name="TranslateButton" Content="Перевести" Width="100" Height="30"/>
        </StackPanel>
        
        <GroupBox Grid.Row="1" Header="Исходный текст" Margin="0,0,0,10">
            <TextBox x:Name="SourceTextBox" 
                     AcceptsReturn="True" 
                     TextWrapping="Wrap" 
                     VerticalScrollBarVisibility="Auto"
                     FontSize="14"/>
        </GroupBox>
    </Grid>
</Window>